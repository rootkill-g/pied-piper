[package]
name = "hello-api"
version = "0.1.0"
edition = "2021"

# WASI P2 components are built as binaries with a main function
# No need for [lib] section anymore

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
